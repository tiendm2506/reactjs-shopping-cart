import React from 'react';
import PropTypes from 'prop-types';

import './style.scss'

Header.propTypes = {
    
};

function Header(props) {
    return (
        <div>
            <h3>Header</h3>
        </div>
    );
}

export default Header;